{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "backend for full-stack-open 2023 part3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:frontend": "rm -rf dist && cd ../../part2/phonebook && npm run build && cp -r dist build",
    "deploy:full": "npm run build:frontend && git add . && git commit -m frontend-build && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adam Wozniak",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}

